pipeline {
    agent any

    stages {
        stage('Run Script') {
            steps {
                sh 'chmod +x scripts/hello.sh'
                sh './scripts/hello.sh'
            }
        }
    }
}
